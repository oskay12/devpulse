# Template only — DO NOT commit a filled-in copy of this file.
#
# Usage:
#   cp k8s/secret.yaml.template k8s/secret.yaml
#   # replace the placeholders below with real values (kept out of git via .gitignore)
#   kubectl apply -f k8s/secret.yaml
#
# Prefer sourcing the real values from AWS Parameter Store / Secrets Manager
# (e.g. via External Secrets Operator) instead of hand-editing this file in
# production clusters.
apiVersion: v1
kind: Secret
metadata:
  name: devpulse-db-secret
  namespace: devpulse
type: Opaque
stringData:
  DB_PASSWORD: "__REPLACE_WITH_RDS_PASSWORD__"
  DatabaseSettings__ConnectionString: "Host=devpulse-rds.xxxxxxxxxxxx.us-east-1.rds.amazonaws.com;Port=5432;Database=devpulse;Username=devpulse_user;Password=__REPLACE_WITH_RDS_PASSWORD__"
